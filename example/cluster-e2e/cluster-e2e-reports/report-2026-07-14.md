# Monibuca v5 Cluster 三节点 E2E — review 修复回归(feature/review-p1-fixes)

- **日期**: 2026-07-14
- **被测分支**: `feature/review-p1-fixes`(基于 `feature/mp4-record-integrity`),5 个修复 commit + 1 个 review 后修复
- **被测二进制**: 在 **172.16.12.82** 上 golang:1.26 容器编译(`CGO_ENABLED=0 GOOS=linux GOARCH=amd64 -tags "cluster sqlite s3"`,88 MB,静态链接),BuildInfo 确认 **`github.com/eanfs/gotask v1.0.5`**(gotask 重入死锁根治版)
- **结论**: ✅ **全部通过**。三节点集群、31 路真实 RTSP 摄像头、跨节点路由/relay、MP4 三模式录制 + S3 上传 + 视频完整性、以及本次 review 修复的三大方向(录制完整性 / 上传解耦 / cluster 死锁与 release-guard)均在真机验证成功。

---

## 1. 拓扑与基础设施

| node | 物理机 | HTTP | gRPC | RTMP | RTSP | 摄像头 |
|---|---|---|---|---|---|---|
| node-1 | 172.16.12.131 | 19080 | 15051 | 21935 | 11554 | 4 (camera1,2,3,7) |
| node-2 | 172.16.12.134 | 19081 | 15052 | 11936 | 11555 | 12 (camera8-16,25,26,32) |
| node-3 | 172.16.12.133 | 19082 | 15053 | 11937 | 11556 | 15 (camera17-24,27-31,34,35) |

**专用 infra(本轮改在 172.16.12.133)**: Consul `:8500`(swr consul 1.22.7-amd64 容器)、MinIO `:29000`(bucket `m7s-records`,数据在 133 `/home`)。各节点本地 sqlite;集群协调走 Consul。

**环境处置说明**:
- 构建只在 82(本地 Docker Desktop 崩溃循环 + 磁盘满,改用 82 的 docker+golang 容器交叉编译)。
- **131 root 分区 100% 满**(生产隐患,/var 63G,大头在 docker)——本轮把专用 consul+minio 移到最闲的 133(/home 202G);node-1 在 131 用 `/home/m7s-cluster` 作工作目录(db/TMPDIR/日志均走 /home),不触碰 131 root 现有数据。
- 133 缺 consul 镜像,经 SWR 登录后 `docker pull consul:1.22.7-amd64` 获取。

## 2. 集群 membership(场景 1)✅
三节点 `/cluster/api/cluster/nodes` 均 `peers=3`;Consul KV `m7s/nodes/{node-1,2,3}` 齐全。

## 3. 拉流 31 路 + KV 注册(场景 2)✅
- node-2/node-3 从本地 sqlite 自动加载各自 pull 配置并自动拉流(12 + 15);node-1(/home 空 db)手动错峰补 4 路。
- **31/31 全部上线**,Consul KV `m7s/streams/` 共 **31 个 key**,属主映射抽检全对:camera1/7→node-1、camera10/16→node-2、camera29/35→node-3。**验证三节点 StreamRegistry acquire 路径(本次 C1 修复)正确。**

## 4. 跨节点路由与 relay
- **T2 302 路由** ✅:node-2 请求 `flv/live/camera1`(node-1 拥有）→ `HTTP 302 Location: http://172.16.12.131:19080/flv/live/camera1`。
- **T3 auto-relay** ✅:node-2 RTSP 订阅 `live/camera1`(node-1 拥有)→ 6s **107 帧**(~18fps)持续供流,跨节点媒体面 relay 成立。

## 5. cluster 死锁回归 + release-guard(T1,本次 C1 修复关键)✅
node-1 拉一个 node-3 已拥有的同名 `live/camera29`:
- **node-1 `/api/stream/list` 连续 6 探全 200 / 7-10ms —— 全程无死锁**(修复前 RC1/RC2 必死锁 stream-list)。
- acquire 失败 → publisher `unpublish` **干净停止**(first-write-wins)。
- **node-3 保留 camera29 属主(KV owner=node-3)** —— acquired/disposed 原子握手 + `releaseOnce` CAS 生效,node-1 失败的 publisher **没有删掉 peer 的 KV 键**(次生 bug 未复现);node-1 无残留。

## 6. MP4 录制 + S3 + 视频完整性(主验证目标)

所有录像直写 S3(MinIO),`File.Close()` 触发上传;校验用 MinIO 取回 + ffprobe + 全帧解码。**录像成品全部保留在 `m7s-records`。**

### T4 — 本地录制 + G.711 音频(node-3,camera27)✅
- S3 `T4-local-camera27/c27.mp4`,**9.6 MiB**。
- ffprobe:视频 h264 2688×1520;**音频 pcm_alaw 8000Hz `channels=1`**(音频修复生效,修复前 channels=0 整文件不可解析);时长 17.9s。
- **moov 在文件头前 64KB**(fast-start / trailer 重写 PrepareFrontMoov 路径)。
- 全帧解码 **exit=0**(仅有已知的 non-monotonic dts warning,不影响解析/播放)。

### T5 — 跨节点 4K 录制(node-1 relay node-3 的 camera29)✅
- node-1 RTSP 订阅触发 auto-relay(relay publisher 经 `activeRelays` 正确识别、跳过 acquire,不被误停)→ camera29 成本地流 → 录 20s。
- S3 `T5-xnode-camera29/c29-4k.mp4`,**5.5 MiB**。
- ffprobe:**h264 3840×2160(4K)**,**402 帧**,20.06s;**moov 在文件头**;**全解码 0 fatal error,exit=0**。
- **验证 co64/大文件写入路径 + trailer 重写 + 跨节点 relay 录制,全部正确。**

### E2 — 并发上传解耦(node-2,本次 U1 修复关键)✅
- node-2 同时录 camera8-12 共 5 路 → **同时停录** 制造并发 trailer+上传。
- S3 上传日志观测到 **`active=4/4`**(满槽并发上传)—— **修复前上传内联在单线程 trailer 队列、恒为 `active=1/4` 严格串行**;本次 U1 把 Close→上传→延迟入库移交独立上传队列,并发度由上传槽位(4)真正决定。
- 5 个对象全部上传成功(E2-camera8..12,457KiB–5.4MiB);延迟入库 `db save RecordStream (deferred) ok` 全部成功(上传成功才写 DB 的语义正确)。

## 7. 日志错误扫描
三节点 error 级日志仅 **RTSP video timeout / RTSP send error / RTMP handshake**——31 路真实摄像头断连重连 + 测试订阅结束的网络噪音,**零条与录制/上传/trailer/cluster 改动相关**。("record error" 计数为 grep `ERR` 误命中 `deferred`,实为 INF 级延迟入库成功日志。)

## 8. 保留的 S3 录制成品(bucket `m7s-records` @ 133:29000)
| key | 大小 | 内容 |
|---|---|---|
| `T4-local-camera27/c27.mp4` | 9.6 MiB | 2K H264 + G.711 channels=1,本地录制 |
| `T5-xnode-camera29/c29-4k.mp4` | 5.5 MiB | 4K H264,跨节点 relay 录制 |
| `E2-camera8..12/*.mp4` | 457KiB–5.4MiB | 5 路并发录制(验证上传解耦)|

MinIO Console: http://172.16.12.133:29001 (admin / m7sm7sm7s)。

## 9. 结论
`feature/review-p1-fixes` 在 3 节点活集群 + 31 路真实摄像头上回归通过。本次 review 修复的三大方向均获真机验证:
- **录制完整性**(mp4):moov-first fast-start、4K/大文件、G.711 channels=1 —— T4/T5 全解码 0 错误。
- **上传解耦**(U1):并发录制停录时上传真正并发(active=4/4),不再被单线程 trailer 队列串行拖死 —— E2 实证。
- **cluster 死锁 + release-guard**(C1):跨节点同名流冲突干净停止不死锁、失败 publisher 不误删 peer 键 —— T1 实证。

无一错误与本次改动相关。
